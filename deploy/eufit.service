[Unit]
Description=EUFit — Flask app (Gunicorn)
After=network.target

[Service]
Type=simple
User=jurandir_fisico
Group=jurandir_fisico
WorkingDirectory=/home/jurandir_fisico/eufit
Environment="PATH=/home/jurandir_fisico/eufit/.venv/bin"
ExecStart=/home/jurandir_fisico/eufit/.venv/bin/gunicorn --workers 2 --bind 127.0.0.1:8000 --timeout 60 --access-logfile - --error-logfile - wsgi:app

Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
